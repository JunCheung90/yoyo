#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../tick/bin/node-tick-processor.js" "$@"
  ret=$?
else 
  node  "$basedir/../tick/bin/node-tick-processor.js" "$@"
  ret=$?
fi
exit $ret
