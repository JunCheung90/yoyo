var sys = require('sys');
var fs = require('fs');
var util = require('util');

var Faker = require('../index');

for (var i = 1; i <= 4; i++) {
	fs.writeFile('../examples/us' + i + '.json',  JSON.stringify(Faker.User.owner()), function () {
		sys.puts("dataSet generated successfully!");
	});
}
